name: Matrix Test

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ${{ matrix.target == 'macos-x64' && 'macos-latest' || 'ubuntu-latest' }}
    strategy:
      matrix:
        target: [linux-x64, macos-x64]
    steps:
      - run: echo "Target is ${{ matrix.target }}"
